type IEmployee = {
    firstName: string;
    lastName: string;
}

type IReview = {
    title: string;
}

type IFeedback = {
    reviewee: string;
    reviewer: string;
    title: string;
    feedback: string;
}